<?xml version="1.0" encoding="utf-8"?>
<layout>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical">


        <ListView
            android:id="@+id/inner_listivew"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="@android:color/transparent"
            android:gravity="center"
            android:horizontalSpacing="@dimen/grid_view_horizontalSpacing"
            android:listSelector="@android:color/transparent"
            android:paddingLeft="@dimen/list_item_left_right_padding"
            android:paddingRight="@dimen/list_item_left_right_padding"
            android:paddingTop="@dimen/list_item_top_bottom_half_padding"
            android:verticalSpacing="@dimen/grid_view_verticalSpacing"/>

    </LinearLayout>
</layout>